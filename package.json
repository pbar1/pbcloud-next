{
  "name": "pbcloud-next",
  "version": "1.0.0",
  "main": "main.js",
  "types": "main.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "import": "npx cdk8s import",
    "synth": "npx cdk8s synth",
    "compile": "npx tsc --build",
    "watch": "npx tsc --build -w",
    "test": "jest",
    "build": "npm run compile && npm run test && npm run synth",
    "qbuild": "npm run compile && npm run synth",
    "upgrade": "npm i cdk8s@latest cdk8s-cli@latest",
    "upgrade:next": "npm i cdk8s@next cdk8s-cli@next"
  },
  "dependencies": {
    "cdk8s": "^2.29.0",
    "cdk8s-plus-25": "^2.16.0",
    "constructs": "^10.2.69",
    "type-fest": "^4.3.1"
  },
  "devDependencies": {
    "@types/jest": "^26",
    "@types/node": "^14",
    "cdk8s-cli": "^2.15.1",
    "jest": "^26",
    "ts-jest": "^26",
    "ts-node": "^10",
    "typescript": "^4.9.5"
  }
}